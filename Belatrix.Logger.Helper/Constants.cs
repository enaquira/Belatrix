namespace Belatrix.Logger.Helper
{
    public static class Constants
    {
        public const string InvalidConfigurationError = "Invalid configuration";
        public const string LogTypeMustBeSpecifiedError = "Error or Warning or Message must be specified";
        public const string NullOrEmptyMessageError = "The message cannot be NULL or empty";
    }
}
